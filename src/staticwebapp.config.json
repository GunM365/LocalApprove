{
    "navigationFallback": {
      "rewrite": "/"
    },
    "routes": [
      {
        "route": "/api/v2/memos/*",
        "allowedMethods": ["*"]
      }
    ],
    "proxies": {
      "api": {
        "matchCondition": {
          "route": "/api/v2/memos/*"
        },
        "backendUri": "https://testfuncapp365.azurewebsites.net/api/v2/memos/"
      }
    }
  }
  